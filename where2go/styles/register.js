import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    title: {
        color: 'gray',
        fontWeight: '700',
        fontSize: 24,
        marginBottom: 4,
        marginLeft: 24
    },
    box: {
        marginTop: 10,
        alignItems: 'center'
    },
});

export default styles;